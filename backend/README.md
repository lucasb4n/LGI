# LGI Backend

Projeto Spring Boot mínimo com endpoints de autenticação (exemplo).

Como rodar:

1. Instale o JDK 17+
2. No diretório `backend` rode:

```
mvn spring-boot:run
```

Os endpoints ficam em `http://localhost:8080/api/login` e `http://localhost:8080/api/register`.
